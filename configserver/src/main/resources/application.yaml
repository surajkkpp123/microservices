server:
  port: 8085

spring:
  application:
    name: config-server
  cloud:
     config:
       server:
         git:
           uri: https://github.com/surajkkpp123/microservice-config.git
           clone-on-start: true
           default-label: main